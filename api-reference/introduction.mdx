---
title: "Introduction"
description: "Ask Wire API endpoints"
---

<Note>
  The Ask Wire API is currently in private beta. If you're interested in using
  the API, please contact us at
  [support@ask-wire.com](mailto:support@ask-wire.com).
</Note>

## Authentication

All API endpoints are authenticated using Bearer tokens that refresh every 24 hours. To obtain a token, send a POST request to the following endpoint:
`https://zeus.ask-wire.com/auth/otp/login` with the following body:

{/* <RequestExample>

```shellscript Auth
curl --location 'https://zeus.ask-wire.com/auth/otp/login' \
--form 'username=""' \
--form 'password=""'
```

</RequestExample>

```json
{
  "access_token": "",
  "token_type": "bearer"
}
``` */}
